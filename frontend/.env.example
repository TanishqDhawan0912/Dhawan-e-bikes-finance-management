# Backend origin only (no trailing slash). Required for Vercel production builds (VERCEL=1).
# Production:
VITE_API_URL=https://dhawan-e-bikes-finance-management.onrender.com

# Local backend (optional — dev server defaults to localhost:5000 if unset):
# VITE_API_URL=http://localhost:5000
