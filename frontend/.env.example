# Backend origin only (no trailing slash). The app appends /api for REST calls.
# Local backend:
# VITE_API_URL=http://localhost:5000
# Deployed backend (replace with your real Render URL):
VITE_API_URL=https://your-backend.onrender.com
